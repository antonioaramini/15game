-Board tests
-CSS to render properly
-Assign CSS variable from js for Board dimension
-Add events to handle input (make Tile trigger an event and then Board is listening to it and applying the logic)
-Attach event to Tile in Board.js after creating the Tile instance
-Remove tilePositions array and directly use matrix